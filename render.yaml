services:
  - type: web
    name: match-my-resume-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    autoDeploy: true
    buildFilter:
      paths:
        - backend/**
    envVars:
      - key: PORT
        value: 10000
